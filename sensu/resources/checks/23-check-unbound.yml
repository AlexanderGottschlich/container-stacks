type: CheckConfig
api_version: core/v2
metadata:
  name: check-unbound
spec:
  command: >-
    check-tcp.rb -h 127.0.0.1 -p 5335 -t 3
  interval: 30
  timeout: 10
  publish: true
  subscriptions:
    - naboo
  runtime_assets:
    - sensu-plugins-tcp
