type: CheckConfig
api_version: core/v2
metadata:
  name: check-collabora
spec:
  command: >-
    check-http.rb -u https://office.nc2.elastic2ls.com/hosting/discovery -t 10
  interval: 60
  timeout: 15
  publish: true
  subscriptions:
    - naboo
  runtime_assets:
    - sensu-plugins-http
